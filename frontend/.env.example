# =====================================================================
# Email-Ops — Frontend environment template
# Copy to .env.local and fill in. Never commit .env.local.
# =====================================================================

# Public base URL of the backend API (used by the browser for SSO + fetches).
# In dev this is usually the backend's local address; in prod, the public host.
NEXT_PUBLIC_API_URL=https://emailops.unicorncommander.ai/api/v1
