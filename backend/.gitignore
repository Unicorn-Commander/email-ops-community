# compiled output
/dist
/node_modules

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
*.launch
.settings/
.vscode/*

# Environment
.env
.env.local
.env.*.local

# Prisma
# Migrations ARE tracked: Email-Ops uses Prisma Migrate (versioned SQL) from
# day one — versioned SQL is the source of truth for RLS, role grants, and the
# tenancy foundation. Do NOT re-add `prisma/migrations/*` here.
